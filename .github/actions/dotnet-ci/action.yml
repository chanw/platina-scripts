name: "DotNet CI"
description: "Builds, tests, packages and publishes a dotnet based solution."

inputs:
  Solution: 
    description: "Optional solution to process, use this if you have multiple solutions."
    required: false
    default: "*.sln"

  DotNetSdkVersion:
    description: "Specific .NET SDK version to use."
    required: false
    default: ""
  WithNuGetCache:
    description: "Whether to cache NuGet packages."
    required: false
    default: ""

runs:
  using: composite
  steps:
    - uses: actions/setup-dotnet@v5
      if: ${{ inputs.DotNetSdkVersion != '' }}
      name: Use specific .NET SDK version ${{ inputs.DotNetSdkVersion }}
      with:
        dotnet-version: ${{ inputs.DotNetSdkVersion }}
        cache: ${{ inputs.WithNuGetCache == 'true' }}

    - name: DotNet Restore
      shell: bash
      run: dotnet restore ${{ inputs.Solution }}

# todo:
# - dotnet tool restore
# - dotnet build
# - dotnet pack
# - dotnet publish
# - before tests steps?
# - dotnet test
# - publish test results
# - after tests steps?
# - dotnet push + dotnet push internal feed?
# - dotnet push symbols?
# - docker login + push to all containers
# - publish pipeline artifacts (packages)